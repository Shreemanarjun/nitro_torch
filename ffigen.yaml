# Run with `dart run ffigen --config ffigen.yaml`.
name: NitroTorchBindings
description: |
  Bindings for `src/nitro_torch.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/nitro_torch_bindings_generated.dart'
headers:
  entry-points:
    - 'src/nitro_torch.h'
  include-directives:
    - 'src/nitro_torch.h'
comments:
  style: any
  length: full
